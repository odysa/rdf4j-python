[project]
name = "rdf4j-python"
version = "0.1.0"
description = "The Python client for RDF4J"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "ruff>=0.11.8",
]
